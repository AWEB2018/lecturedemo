{
  "name": "project1",
  "version": "1.0.0",
  "description": ">> INSTALL NODEJS >> node -v >> npm -v >> INSTALL VSCODE >> Open VSCODE and Create Project. >> EDITOR PREFS     >> COLOR THEME     >> Create File & Folder >> OPEN TERMINAL     RUN THE COMMAND     >> npm init",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "lite-server ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.1.0",
    "jquery": "^3.3.1",
    "lite-server": "^2.3.0",
    "open-iconic": "^1.1.1"
  }
}
